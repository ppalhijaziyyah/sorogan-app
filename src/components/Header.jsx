import React from 'react';

const Header = () => {
  // The Header is now only a placeholder for potential future content on the learning page.
  // The title is now managed within LearningPage.jsx and the theme toggle is in Footer.jsx.
  return (
    <header className="p-4 shadow-md bg-white/50 dark:bg-gray-900/50 backdrop-blur-sm">
      {/* This header is currently empty as its content has been moved or is context-specific. */}
      {/* It is kept for structural consistency and future use. */}
    </header>
  );
};

export default Header;